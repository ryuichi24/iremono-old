{
  "name": "iremono",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": " yarn concurrently \"yarn dev-server\" \"yarn dev-web\"",
    "dev-web": "export $(cat .env.dev | grep -v \"#\" | xargs) && yarn workspace @iremono/web dev",
    "dev-server": "export $(cat .env.dev | grep -v \"#\" | xargs) && yarn workspace @iremono/server dev",
    "init-mysql-db": "export $(cat .env.dev | grep -v \"#\" | xargs) && yarn workspace @iremono/backend-core init-mysql-db",
    "init-mysql-tables": "export $(cat .env.dev | grep -v \"#\" | xargs) && yarn workspace @iremono/backend-core init-mysql-tables",
    "cleanup-mysql-db": "export $(cat .env.dev | grep -v \"#\" | xargs) && yarn workspace @iremono/backend-core cleanup-mysql-db"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
